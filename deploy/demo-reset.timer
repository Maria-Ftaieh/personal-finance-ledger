[Unit]
Description=Reset the personal-finance-ledger demo nightly

[Timer]
OnCalendar=*-*-* 04:00:00
# Catch up if the host was off at 04:00 rather than skipping a day.
Persistent=true
RandomizedDelaySec=600

[Install]
WantedBy=timers.target
